# Frontend Environment Variables

# API URL - Use relative path for production, localhost for development
# Production (Vercel): /api
# Development: http://localhost:8000/api
NEXT_PUBLIC_API_URL=/api

# Canonical site URL for metadata, Open Graph, and sitemap (no trailing slash)
# Production: https://your-domain.com
# Leave unset to use default (raghavchawla.dev)
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
